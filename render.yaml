services:
  - type: web
    name: akhi-ex-responds
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /tmp/data
    disk:
      name: data
      mountPath: /tmp/data
      sizeGB: 1